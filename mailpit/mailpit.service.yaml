apiVersion: v1
kind: Service
metadata:
  name: mailpit
  namespace: domestic
  labels:
    app: mailpit
spec:
  type: LoadBalancer
  selector:
    app: mailpit
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
    - name: ui
      port: 8025
      targetPort: 8025
