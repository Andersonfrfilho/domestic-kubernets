apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: domestic
  labels:
    app: minio
type: Opaque
# IMPORTANT: Replace placeholder values before applying.
stringData:
  MINIO_ROOT_USER: "domestic"
  MINIO_ROOT_PASSWORD: "minioadmin"
