apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secret
  namespace: domestic
  labels:
    app: rabbitmq
type: Opaque
# IMPORTANT: Replace placeholder values before applying.
stringData:
  RABBITMQ_DEFAULT_USER: "domestic"
  RABBITMQ_DEFAULT_PASS: "backendapi123"
