apiVersion: v1
kind: Service
metadata:
  name: postgres-keycloak
  namespace: domestic
  labels:
    app: postgres-keycloak
spec:
  selector:
    app: postgres-keycloak
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
