apiVersion: v1
kind: Service
metadata:
  name: bff
  namespace: domestic
  labels:
    app: bff
spec:
  selector:
    app: bff
  type: ClusterIP
  ports:
    - name: http
      port: 3001
      targetPort: 3001
