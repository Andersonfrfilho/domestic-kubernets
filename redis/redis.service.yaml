apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: domestic
  labels:
    app: redis
spec:
  selector:
    app: redis
  type: LoadBalancer
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
