apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: domestic
  labels:
    app: postgres
data:
  POSTGRES_DB: "domestic_postgres"
  PGDATA: "/var/lib/postgresql/data/pgdata"
