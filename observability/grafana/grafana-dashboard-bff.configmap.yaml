apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-bff
  namespace: domestic
  labels:
    app: grafana
data:
  bff.json: |
    {
      "title": "Domestic BFF",
      "uid": "domestic-bff",
      "version": 1,
      "refresh": "30s",
      "time": { "from": "now-1h", "to": "now" },
      "templating": {
        "list": [
          {
            "name": "interval",
            "type": "interval",
            "query": "1m,5m,10m,30m",
            "current": { "value": "5m" }
          }
        ]
      },
      "panels": [
        {
          "id": 1,
          "title": "Request Rate (req/s)",
          "type": "timeseries",
          "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 },
          "targets": [
            {
              "expr": "sum(rate(http_requests_total{service=\"domestic-bff\"}[$interval])) by (route)",
              "legendFormat": "{{route}}",
              "datasource": "Prometheus"
            }
          ]
        },
        {
          "id": 2,
          "title": "Error Rate (5xx %)",
          "type": "timeseries",
          "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 },
          "targets": [
            {
              "expr": "sum(rate(http_requests_total{service=\"domestic-bff\",status_code=~\"5..\"}[$interval])) / sum(rate(http_requests_total{service=\"domestic-bff\"}[$interval])) * 100",
              "legendFormat": "Error %",
              "datasource": "Prometheus"
            }
          ]
        },
        {
          "id": 3,
          "title": "Latency P50 / P95 / P99 (s)",
          "type": "timeseries",
          "gridPos": { "x": 0, "y": 8, "w": 12, "h": 8 },
          "targets": [
            {
              "expr": "histogram_quantile(0.50, sum(rate(http_request_duration_seconds_bucket{service=\"domestic-bff\"}[$interval])) by (le))",
              "legendFormat": "p50",
              "datasource": "Prometheus"
            },
            {
              "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{service=\"domestic-bff\"}[$interval])) by (le))",
              "legendFormat": "p95",
              "datasource": "Prometheus"
            },
            {
              "expr": "histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket{service=\"domestic-bff\"}[$interval])) by (le))",
              "legendFormat": "p99",
              "datasource": "Prometheus"
            }
          ]
        },
        {
          "id": 4,
          "title": "WebSocket Connections",
          "type": "timeseries",
          "gridPos": { "x": 12, "y": 8, "w": 12, "h": 8 },
          "targets": [
            {
              "expr": "nodejs_active_handles_total{job=\"domestic-bff\"}",
              "legendFormat": "Active Handles",
              "datasource": "Prometheus"
            }
          ]
        },
        {
          "id": 5,
          "title": "Node.js Heap Used (bytes)",
          "type": "timeseries",
          "gridPos": { "x": 0, "y": 16, "w": 12, "h": 8 },
          "targets": [
            {
              "expr": "nodejs_heap_size_used_bytes{job=\"domestic-bff\"}",
              "legendFormat": "Heap Used",
              "datasource": "Prometheus"
            }
          ]
        },
        {
          "id": 6,
          "title": "Total Requests",
          "type": "stat",
          "gridPos": { "x": 12, "y": 16, "w": 6, "h": 8 },
          "targets": [
            {
              "expr": "sum(http_requests_total{service=\"domestic-bff\"})",
              "legendFormat": "Total",
              "datasource": "Prometheus"
            }
          ]
        },
        {
          "id": 7,
          "title": "BFF Logs",
          "type": "logs",
          "gridPos": { "x": 0, "y": 24, "w": 24, "h": 10 },
          "targets": [
            {
              "expr": "{namespace=\"domestic\", app=\"bff\"}",
              "legendFormat": "",
              "datasource": "Loki"
            }
          ],
          "options": {
            "showLabels": false,
            "showTime": true,
            "sortOrder": "Descending",
            "wrapLogMessage": true
          }
        }
      ]
    }
