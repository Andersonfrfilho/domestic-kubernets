apiVersion: v1
kind: Secret
metadata:
  name: worker-secret
  namespace: domestic
  labels:
    app: worker
type: Opaque
# IMPORTANT: Replace placeholder values before applying.
# Use os mesmos valores dos outros secrets (postgres-secret, minio-secret, etc.)
stringData:
  DATABASE_POSTGRES_USER: "domestic"
  DATABASE_POSTGRES_PASSWORD: "postgres1234"
  QUEUE_RABBITMQ_USER: "domestic"
  QUEUE_RABBITMQ_PASS: "backendapi123"
  STORAGE_MINIO_ACCESS_KEY: "domestic"
  STORAGE_MINIO_SECRET_KEY: "minioadmin"
